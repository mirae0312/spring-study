<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<select id="selectBoardList" resultType="board">
		select 
		    b.*,
		    (select count(*) from attachment where board_no = b.no) attachment_count
		from 
		    board b
		order by 
		    no desc
	</select>
	<select id="selectTotalBoardCount" resultType="_int">
		select
			count(*)
		from
			board
	</select>
	
	<insert id="insertBoard">
		insert into
			board(no, title, member_id, content)
		values(
			seq_board_no.nextval,
			#{title},
			#{memberId},
			#{content}
		)
		<selectKey order="AFTER" resultType="_int" keyProperty="no">
			select
				seq_board_no.currval
			from
				dual
		</selectKey>	
	</insert>
	<insert id="insertAttachment">
		insert into
			attachment(no, board_no, original_filename, renamed_filename)
		values(
			seq_attachment_no.nextval,
			#{boardNo},
			#{originalFilename},
			#{renamedFilename}
		)
	</insert>
	
	<select id="selectOneBoard" resultType="board">
		select
			*
		from
			board
		where
			no = #{no}
	</select>
	<select id="selectAttachmentListByBoardList" resultType="attachment">
		select
			*
		from
			attachment
		where
			board_no = #{boardNo}
	</select>
</mapper>